// config.js - Configuração centralizada do backend
// Lê window.API_BASE definido pelo config.js raíz (/assets/config.js)
// Este arquivo é importado como ES Module pelos scripts nativos (driver-native, graphic-native).

export const API_BASE = window.API_BASE || '';
